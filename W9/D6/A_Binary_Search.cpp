#include <bits/stdc++.h>
using namespace std;

int main()
{

    int n, k;
    cin >> n >> k;
    vector<int> v(n);
    for (int i = 0; i < n; i++)
    {
        cin >> v[i];
    }
    for (int i = 0; i < k; i++)
    {
        int key = v[i];
        int l = 0;
        int r = n - 1;
        int mid = 0 ;
        while (l<=r )
        {
            
            mid = (l+r)/2;
            if 
        }
        
        
  
    }

    return 0;
}
