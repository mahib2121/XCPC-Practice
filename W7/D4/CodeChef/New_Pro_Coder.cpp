#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n, m;
    cin >> n >> m;

   // int k = m * 2;
    if (m >= (n+1) / 2)
    {
        cout << "NEWBIE" << endl;
    }

    else
    {
        cout << "PRO" << endl;
    }
}
